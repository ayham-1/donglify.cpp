project('donglify', 'cpp',
  version : '0.1',
  default_options : ['warning_level=3',
                     'cpp_std=c++23'])

exe = executable('donglify', 'src/donglify.cpp',
  install : true)

test('basic', exe)
